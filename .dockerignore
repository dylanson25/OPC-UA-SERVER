node_modules
dist
build

.git
.gitignore

# Never bake local/secret config into the image; .env.example is fine to keep
.env
.env.*
!.env.example

logs
coverage
.vitest
*.test.ts.snap

# Never bake a server identity cert/key into the image — mount it as a volume instead
certs

tests

.vscode
.idea

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

README.md
