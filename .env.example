PORT = 48040
RESOURCEPATH = '/UA/'
HOSTNAME = '127.0.0.1'
PRODUCTNAME = 'OPCUA-Server'
NODE_ENV = 'development'
# Optional: interval (ms) for periodic metrics summary logging. Unset/0 disables it.
METRICS_LOG_INTERVAL_MS = 0
# Optional: comma-separated NodeSet2 XML files to import alongside the standard UA
# nodeset (see `start --nodeset-file`), e.g. NODESET_FILES=./nodeset.xml,./other.xml
NODESET_FILES =